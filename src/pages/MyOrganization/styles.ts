import styled from 'styled-components'
import { PageWrapper } from '~/themes'

export const MyOrganizationWrapper = styled(PageWrapper)`
  .btn-edit-icon {
    position: absolute;
    bottom: -10%;
    right: -20%;
    font-size: 57px;
    cursor: pointer;
  }
`
